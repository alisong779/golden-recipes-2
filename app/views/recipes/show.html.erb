
<%= render 'recipe', locals: @comment %>
  <% if current_user.id == @recipe.user_id%>
    <%= link_to "Edit Recipe", edit_recipe_path %>|
    <%= link_to "Delete Recipe", recipe_path, method: :delete, data: {confirm: "You sure?"} %>
  <% end %>
