
<%= render 'recipe', locals: @comment %>
  <% if current_user.id == @recipe.user_id%>
    <%= link_to "Edit", edit_recipe_path %>|
    <%= link_to "Delete", recipe_path, method: :delete, data: {confirm: "You sure?"} %>
  <% end %>
